services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: tol
      POSTGRES_PASSWORD: tol
      POSTGRES_DB: treeoflife
    ports: ["5433:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes: { pgdata: {} }